{
  "_comment": [
    "Every passkey and gate in one place. Previously each was a string literal",
    "inside the page it guarded, so changing one meant hunting through HTML.",
    "",
    "READ THIS BEFORE TRUSTING IT: these are client-side gates in a public",
    "repository. Anyone can read this file, or the page source, and learn every",
    "key here. They keep an admin tool from being opened by accident or by a",
    "casual visitor — they are NOT access control, and nothing genuinely",
    "sensitive should sit behind them. Real protection needs a server, or the",
    "Firebase accounts already being wired up in dge/js/user-auth.js.",
    "",
    "Keys are compared case-insensitively, trimmed.",
    "",
    "Change a key here and it takes effect on next load. Anyone already holding",
    "a session flag stays in until their tab closes; bump sessionFlag alongside",
    "the key to evict them immediately."
  ],

  "gates": {
    "superadmin": {
      "label": "Super admin",
      "key": "SHRI108",
      "sessionFlag": "dge.admin.ok",
      "grants": ["kosha", "holy-places", "content-provenance"],
      "note": "Also satisfied by localStorage is_superadmin === 'true', which the account system sets."
    },
    "audioadmin": {
      "label": "Audio admin",
      "key": "AUDIOADMIN",
      "sessionFlag": "dge.audioadmin.ok",
      "grants": ["audio"],
      "note": "Separate key so audio work can be delegated without handing over the super-admin key."
    },
    "capture": {
      "label": "Capture and convert",
      "key": null,
      "sessionFlag": null,
      "grants": ["dasa-capture", "convert"],
      "note": "No passkey by design — these push straight to GitHub, so they require localStorage is_superadmin === 'true', set only by the account system."
    }
  }
}
