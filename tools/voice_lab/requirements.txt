# Track A (voice_transform.py) — lightweight DSP, CPU-only, no models:
numpy
scipy
# Track B (clone_knn_vc.py) — optional, AI zero-shot voice conversion (heavier):
# torch
# torchaudio
# torchcodec
# soundfile
# Track C (tts_clone.py) — optional, zero-shot text-to-speech (heaviest, ~1.8GB
# model download; needs torch/torchaudio too, shared with Track B):
# TTS
