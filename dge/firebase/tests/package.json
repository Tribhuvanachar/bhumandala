{
  "name": "dge-firebase-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Tests for DGE's Firebase layer (security rules, OTP logic, WhatsApp client, auth UI).",
  "scripts": {
    "test": "node --test",
    "test:rules": "firebase emulators:exec --only firestore --project dge-test \"node --test rules.spec.js\"",
    "test:all": "npm test && npm run test:rules && npm run test:e2e",
    "test:e2e": "rm -f /tmp/dge-otp-echo.txt && cd .. && DGE_OTP_ECHO_FILE=/tmp/dge-otp-echo.txt tests/node_modules/.bin/firebase emulators:exec --only functions,firestore,auth --project dge-test \"cd tests && node --test e2e.spec.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^15.0.0"
  }
}
