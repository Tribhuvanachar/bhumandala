# Vision-only page OCR -> committed JSON on a staging branch.
#
# Runs Google Cloud Vision (DOCUMENT_TEXT_DETECTION) over a PDF page range and commits the raw
# per-page result to branch  ocr-staging/<work_slug>  under dge/data/ocr_staging/<work_slug>/.
# No Gemini call is made (₹0 Gemini). The local importer for the work merges this with a
# Tesseract pass, classifies agreement, and emits the human/Gemini verification queue.
# Needs the VISION_API_KEY repository secret (Settings -> Secrets and variables -> Actions).
name: OCR — Vision pages to staging branch

on:
  workflow_dispatch:
    inputs:
      pdf_url:
        description: "Direct-download URL of the PDF (e.g. https://drive.google.com/uc?export=download&id=...)"
        required: true
      start_page:
        description: "First PDF page (1-indexed, inclusive)"
        required: true
      end_page:
        description: "Last PDF page (inclusive)"
        required: true
      work_slug:
        description: "Folder under dge/data/ocr_staging/ and branch suffix, e.g. bhagavata_saroddhara"
        required: true
      dpi:
        description: "Render resolution"
        required: true
        default: "200"
      lang_hints:
        description: "Vision language hints, comma-separated"
        required: true
        default: "sa,kn,hi"

permissions:
  contents: write

jobs:
  vision:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install pymupdf
      - name: Download the PDF
        run: |
          set -euo pipefail
          curl -sS -L --max-time 600 -o source.pdf "${{ inputs.pdf_url }}"
          file source.pdf | grep -qi pdf || { echo "::error::Downloaded file is not a PDF (is the link public?)"; head -c 400 source.pdf; exit 1; }
      - name: Vision OCR
        env:
          VISION_API_KEY: ${{ secrets.VISION_API_KEY }}
        run: |
          set -euo pipefail
          [ -n "$VISION_API_KEY" ] || { echo "::error::VISION_API_KEY secret is not set"; exit 1; }
          python3 tools/vision_ocr_pages.py --pdf source.pdf --start "${{ inputs.start_page }}" --end "${{ inputs.end_page }}" \
            --dpi "${{ inputs.dpi }}" --lang "${{ inputs.lang_hints }}" \
            --out "dge/data/ocr_staging/${{ inputs.work_slug }}/vision_pages_${{ inputs.start_page }}-${{ inputs.end_page }}.json"
      - name: Commit to the staging branch
        env:
          GIT_AUTHOR_NAME: github-actions[bot]
          GIT_AUTHOR_EMAIL: github-actions[bot]@users.noreply.github.com
          GIT_COMMITTER_NAME: github-actions[bot]
          GIT_COMMITTER_EMAIL: github-actions[bot]@users.noreply.github.com
        run: |
          set -euo pipefail
          BR="ocr-staging/${{ inputs.work_slug }}"
          git fetch origin "$BR" && git checkout -B "$BR" "origin/$BR" || git checkout -B "$BR"
          git add "dge/data/ocr_staging/${{ inputs.work_slug }}"
          git commit -m "ocr: Vision pages ${{ inputs.start_page }}-${{ inputs.end_page }} for ${{ inputs.work_slug }}" || { echo "nothing to commit"; exit 0; }
          git push "https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}" "HEAD:$BR"
